# CMake out-of-source build tree.
build/
.cache/
compile_commands.json

# The Go application this branch replaces; its binary may still be lying around.
/nota

# Local agent tooling and working artifacts, not project source.
.claude/
.lavish/

# makepkg working tree, produced by building PKGBUILD in place.
/nota-git/
/pkg/
/src/nota-git/
*.pkg.tar.zst
*.pkg.tar.zst.sig
